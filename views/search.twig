<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
   
</head>
<body>
{% include 'navbar.twig' %} 
<nav class="navbar search navbar-light bg-white">
  <div class="container-fluid ">
    <form method="GET" action="/search" class="d-flex">
      <input type="text" class="form-control me-2" name="username" />
      <input type="submit" value="Rechercher"  class="btn btn-outline-success" id="recherche" />
    </form>
  </div>
</nav>
  <div class="resultat"><H4>Résultats de La Recherche
  </div>
  <div class="row  resultat_search ">
    {% if pr   %} 
      {% for produit in pr %}
        <div class="col product d-flex">
            <div class="col-md-2 shadow imageproduct "> 
              <img src="{{produit.url}}"/>
            </div>
            <div class="col-md-4 p-4 shadow ">
              <a href="/prodcuisine/{{ produit.id }}">{{produit.nom}}</a>
              <h6 class="prix mt-3">{{produit.prix}} TND</h6>
              <ul class="product_result">
                {% if produit.Capacite %}<li> Capacité: <span>{{produit.Capacite}}</span></li>{% endif %}
                {% if produit.Materiau %} <li>Matériau: <span>{{produit.Materiau}} </span></li>{% endif %}
                {% if produit.Diametre %} <li>Diametre: <span>{{produit.Diametre}} </span></li>{% endif %}
              </ul>
            </div>
        </div> 
      {% endfor %}
    {%elseif prelectro %}
      {% for produit in prelectro %}
          <div class="col d-flex product">
            <div class="col-md-2   imageproduct "> 
            <img src="{{produit.url}}"/>
            </div>
            <div class="col-md-4  p-3  shadow ">
             <a href="/product/{{ produit.id }}">{{produit.nom}}</a>
              <h6 class="prix">{{produit.prix}} TND</h6>
              <ul class="product_result">
                <li> Puissance: <span>{{produit.Puissance}}W</span></li>
                <li>Pression: <span>{{produit.Pression}} bar</span></li>
              </ul>
            </div>
          </div>
      {% endfor %}


     {%elseif prpc %}
      {% for produit in prpc %}
          <div class="col d-flex product">
            <div class="col-md-2  imageproduct "> 
              <img src="{{produit.url}}"/>
            </div>
            <div class="col-md-6  p-3  shadow ">
             <a href="/prod_pc/{{ produit.id }}">{{produit.nom}}</a>
              <h6 class="prix">{{produit.prix}} TND</h6>
              <ul class="product_result">
                  <li> Processeur: <span>{{produit.Processeur}}W</span></li>
                  <li>Disque dur: <span>{{produit.Disque_dur}} </span></li>
                  <li>RAM: <span>{{produit.RAM}} </span></li>
              </ul>
            </div>
          </div>
      {% endfor %}
        {%else%}
        <h3 class="m-4 text-danger">   Aucun resultat trouvé </h3>
   {% endif %}
  </div>
  {% include 'footer.twig' %} 
     
  </body>
</html>
