<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</head>
<body>
  {% include 'navbar.twig' %} 
  <nav class="navbar search navbar-light bg-white">
    <div class="container-fluid ">
      <form method="GET" action="/search" class="d-flex">
        <input type="text" class="form-control me-2" name="data" />
        <input type="submit" value="Rechercher"  class="btn btn-outline-success" id="recherche" />
      </form>
    </div>
  </nav>
  <div class="resultat"><H4>Résultats de La Recherche
  </div>
  <div class="row  m-4 ">
    {% if produit %} 
      {% for p in produit %}
        <div class=" card col-md-2 product ">
          <div class="card-header shadow "> 
            <img src="{{p.url}}"/>
          </div>
          <div class="card-body p-3 shadow ">
            <a href="/prodcuisine/{{ p.id }}">{{p.nom}}</a>
          </div>
        </div> 
      {% endfor %}
    {% endif %}
    {% if el %} 
      {% for p in el %}
        <div class="card col-md-2 product d-flex">
          <div class="card-header shadow  "> 
            <img src="{{p.url}}"/>
          </div>
          <div class="card-body p-4 shadow ">
            <a href="/product/{{ p.id }}">{{p.nom}}</a>
          </div>
        </div> 
      {% endfor %}
    {% endif %}
    {% if p %} 
      {% for pc in p %}
        <div class="card col-md-2 product d-flex">
          <div class="card-header shadow  "> 
            <img src="{{pc.url}}"/>
          </div>
          <div class="card-body pc-4 shadow ">
            <a href="/prod_pc/{{ pc.id }}">{{pc.nom}}</a>
          </div>
        </div> 
      {% endfor %}
    {% endif %} 
    {%if p is empty and el is empty and produit is empty %}
    <div>
    <h4> Oups....Aucun resultat trouvé </h4>
    </div>
    {% endif %}
  </div>
    {% include 'footer.twig' %}   
</body>
</html>
